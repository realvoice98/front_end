e.stopPropagation 이라는 함수를 사용하면,
이벤트 버블링을 막을 수 있다.

리액트는 사이트 발행 전에 
Html js css 파일을 압축함 (bundling)이라고하는데, 
src폴더 안의 파일들만 압축이되고,
public폴더 안의 파일들은 압축되지 않는 그대로 상태를 유지함.


props로는 원하는 값을 보내서 컴포넌트의 값을 쉽게 조정할 수 있음. << 중요!!!
